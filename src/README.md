# src 底层模块说明

`src/` 保存会被多个脚本复用的模型和物理计算代码。这里是公共库，不建议把一次性分析逻辑继续写入该目录。

| 目录 | 作用 |
| --- | --- |
| `models/` | PyTorch 模型结构和统一预测封装。 |
| `physics/` | 线路静态参数读取及列车牵引机械能仿真。 |
| `strategy/` | 预留的运行策略公共模块，目前尚未形成独立实现。 |

业务流程入口仍为 `scripts_new/main.py`，`src` 中的模块应由脚本导入而不是单独运行。
